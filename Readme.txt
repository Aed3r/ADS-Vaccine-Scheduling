1.Offline algorithm, put the input file in the root directory and name it "offline_instance" to 
run the offline.py file, and the output file name is "offline_result".

2.The online algorithm can run the file by entering the input file path in the .py file, and the
 output result is displayed on the console.

